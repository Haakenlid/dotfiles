# ~/.tmuxinator/blog.yml

name: blog
root: ~/projects/hken.no
pre_window: source ~/.virtualenvs/blog/bin/activate
startup_window: editor

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
        - git status
  - jupyter:
      panes:
        - cd $HOME/projects/hken.no/pages/
        - cd $HOME/projects/hken.no/pages/
